using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class as12 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    //kore 한글좋아 나이스
    // Update is called once per frame
    void Update()
    {
        
    }
}
